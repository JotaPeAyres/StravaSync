# Arquivos de planejamento (mantidos apenas localmente, fora do versionamento)
PROJECT_SCOPE.md
ARCHITECTURE.md
TASKS.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Ambiente e segredos
.env
# Cadastro dos participantes: contém os refresh_tokens de cada corredor
corredores.toml

# Dados de runtime (a planilha-template versionada fica na raiz do repo)
data/*.db
data/*.xlsx
data/*.log
data/*.log.*
!data/.gitkeep
